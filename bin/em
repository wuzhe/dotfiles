#!/bin/sh

# ${HOME}/bin/em
#if test "$DISPLAY" ; then
#  nohup `which emacsclient.emacs-snapshot` --alternate-editor="" -c "$@"# > /dev/null 2>&1
#else
  exec `which emacs-snapshot` -nw --alternate-editor="" -c "$@"
#fi
